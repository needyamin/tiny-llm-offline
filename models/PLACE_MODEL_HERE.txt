Docker: the image downloads GPT-Neo 125M during "docker compose build" (no host models/ needed).
Without Docker: pip install huggingface_hub && python scripts/download_model.py
  to create models/gpt-neo-125m/ locally.
